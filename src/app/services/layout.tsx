import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Services — Devion",
  description: "From fullstack web apps to AI-powered systems — we deliver production-ready solutions that drive real results.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}
