import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Process — Devion",
  description: "A transparent, collaborative process that delivers results. From idea to launch in 7 proven steps.",
}

export default function ProcessLayout({ children }: { children: React.ReactNode }) {
  return children
}
